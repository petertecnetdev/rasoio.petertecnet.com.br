name: Deploy VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.repository }}-main
  cancel-in-progress: true

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "VPS deployment temporarily held during production service recovery."
